# 麻将计分微信小程序完整项目文档

> 项目名称建议：麻将计分助手  
> 技术栈：uni-app + Vue 3 + Pinia + Spring Boot 3.x + MySQL 8 + WebSocket  
> 输出目标：作为 Codex / Cursor / Claude Code 等 AI Coding 工具的项目开发说明书。  
> 当前阶段：MVP + V1 增强功能设计。

---

## 1. 项目概述

本项目是一个面向线下朋友麻将场景的微信小程序，用于完成开房间、邀请玩家、多人实时计分、历史战绩查询、排行榜、规则说明等功能。

项目的核心不是自动识别麻将番型，而是提供一个灵活、低门槛、多人协作的手动计分系统。

### 1.1 核心定位

- 面向线下麻将局。
- 玩家通过微信小程序进入同一个房间。
- 每名玩家只能提交“自己输给谁多少分”。
- 系统根据所有玩家提交的输分记录自动计算每轮净分。
- 支持 WebSocket 实时同步房间状态。
- 支持房主代提交、踢人、转让房主、关闭房间、重新打开房间。
- 支持座位继承，即新玩家加入空座位后继承该座位当前分数。
- 个人战绩只统计用户实际参与轮次，不继承上一个玩家的历史战绩。
- 支持历史总榜、月度榜、个人胜率、盈亏比等统计。

---

## 2. 技术栈

### 2.1 前端

| 模块 | 选择 |
|---|---|
| 框架 | uni-app |
| 语法 | Vue 3 |
| 状态管理 | Pinia |
| 平台 | 微信小程序优先 |
| UI 风格 | 蓝白简洁工具风 |
| UI 组件 | 尽量少用第三方 UI 库，优先自定义基础组件 |
| HTTP 请求 | 统一封装 `utils/request.js` |
| WebSocket | 统一封装 `utils/socket.js` |
| 本地存储 | `uni.setStorageSync` / `uni.getStorageSync` |

### 2.2 后端

| 模块 | 选择 |
|---|---|
| Java | Java 17 |
| Spring Boot | Spring Boot 3.x |
| ORM | MyBatis-Plus |
| 数据库 | MySQL 8 |
| 缓存 | Redis 预留，MVP 不强依赖 |
| 鉴权 | JWT |
| WebSocket | Spring WebSocket 原生方案 |
| API 文档 | Knife4j / Swagger |
| 构建工具 | Maven |
| 定时任务 | Spring `@Scheduled` |
| 部署 | Java 后端服务器 + HTTPS/WSS 域名 |

### 2.3 Redis 策略

MVP 阶段不强依赖 Redis，避免部署复杂度过高。

后续 V1/V2 可将 Redis 用于：

- 房间状态缓存。
- WebSocket 多实例广播。
- 排行榜缓存。
- 自动关闭房间扫描优化。
- 防止频繁查询 MySQL。

---

## 3. 功能范围

## 3.1 MVP 必做功能

### 用户模块

- 微信静默登录。
- 手机号绑定。
- 手机号首次绑定后第一版不支持换绑。
- 全局昵称允许修改。
- 显示微信头像和昵称。

### 房间模块

- 创建房间。
- 6 位数字房间号。
- 默认房间名称：`麻将房间 + 房间号`。
- 支持 2-4 人房间。
- 座位固定为：东、南、西、北。
- 创建房间时设置：
  - 房间人数。
  - 初始分，所有座位相同，默认 0。
- 加入房间：
  - 房间号加入。
  - 微信分享卡片进入。
  - 二维码进入。
- 多个空位时玩家自己选择座位。
- 必须满员才能开始游戏。
- 房间状态：`WAITING / PLAYING / CLOSED`。
- 房主可以关闭房间。
- 房主可以重新打开 CLOSED 房间。
- 关闭后所有参与玩家仍可查看历史。

### 座位继承

- 分数绑定在座位上。
- 新玩家加入空座位后继承该座位当前分数。
- 新玩家个人战绩只从加入后的下一轮开始统计。
- 被踢或主动退出后，座位变为空位并保留座位分数。
- 玩家退出或被踢，立即结束个人统计周期。
- 玩家在某轮进行中退出：
  - 该玩家本轮默认不输钱。
  - 其他玩家仍可选择输给该玩家。
  - 该玩家本轮收到的分数计入个人战绩。
  - 从下一轮开始不再参与个人统计。

### 计分模块

- 每一轮每个玩家可以提交 0 条或多条“输分记录”。
- 每条输分记录包含：
  - 输给哪个玩家。
  - 输多少分。
  - 可选备注。
- 玩家不能主动给自己加分。
- 玩家不能输给自己。
- 分数只允许正整数。
- 不输不赢表示本轮没有输分记录。
- 所有人提交后自动结算并进入下一轮。
- 自动下一轮后弹出上一轮结果摘要。
- 玩家提交后，在本轮结算前可以反复修改自己的提交。
- 本轮未结算前，所有人可以看到所有人的提交内容。
- 玩家修改提交后通过 WebSocket 实时广播，并显示“某某修改了本轮提交”提示。

### 房主能力

- 开始游戏。
- 关闭房间。
- 重新打开房间。
- 踢人。
- 转让房主。
- 房主主动退出时，自动转让给房间内最早加入的玩家。
- 若房主是唯一玩家，退出时房间关闭，房主仍保留为房间所有者。
- 代提交：
  - 房主可以代提交任何未提交玩家。
  - 不能覆盖已经提交的玩家内容。
  - 代提交需要在明细中显示。
- 强制结算：
  - 房主可以将未提交玩家设置为“不输不赢”，并完成结算。
- 历史局管理：
  - 房主可以修改、删除、撤销历史局。
  - 修改或删除历史局后，必须自动重算该房间所有后续轮次、座位分、个人战绩、排行榜。
  - 历史局修改范围为所有历史局。
- 所有房主操作需要记录操作日志。

### 战绩与排行榜

- 当前房间排行榜：
  - 按当前玩家个人参与分排序。
  - 不展示座位分。
  - 空座位不参与当前排行榜。
- 首页历史总榜：
  - 按用户累计个人净分排序。
  - 展示前 10 名。
- 首页月度榜：
  - 按本月用户累计个人净分排序。
  - 展示前 10 名。
- 个人战绩页：
  - 总房间数。
  - 总轮数。
  - 总净分。
  - 平均每轮得分。
  - 单轮最高得分。
  - 单轮最低得分。
  - 单轮胜率。
  - 房间胜率。
  - 盈亏比。
  - 最近房间。
  - 支持全部 / 本月 / 本周筛选。

### 规则页

- 第一版静态占位。
- 内容包括：
  - 基础术语。
  - 常见计分说明。
  - 当前版本暂不支持自动番型计算。

### WebSocket

- 进入房间页后建立 WebSocket。
- 状态变化实时广播。
- 支持断线自动重连。
- 保留兜底轮询，例如 15 秒一次拉取房间快照。
- 离开房间页后断开 WebSocket。

### 自动关闭房间

- 每 5 分钟扫描一次 PLAYING 房间。
- 如果距离最后一轮成功结算时间超过 1 小时，则自动关闭房间。
- 如果房间开始后没有任何成功结算轮次，则以当前 session 开始时间作为判断时间。
- 自动关闭后状态变为 CLOSED。
- 房主可重新打开，继续原座位分数和原轮次。

---

## 3.2 V1 增强功能

- Redis 缓存。
- WebSocket 多实例广播。
- 排行榜缓存。
- 操作日志前端展示。
- 规则内容改为数据库或 Markdown 管理。
- 二维码生成接入微信小程序码接口。
- 更细粒度的战绩图表。
- 房间搜索。
- 好友维度统计。
- 管理后台。

---

## 4. 核心业务规则

## 4.1 用户规则

1. 用户首次进入小程序，先执行微信静默登录。
2. 如果用户未绑定手机号，必须进入手机号绑定页。
3. 用户必须绑定手机号后才能使用任何核心功能。
4. 第一版手机号绑定后不支持换绑。
5. 用户全局昵称可以修改。
6. 用户在每个房间内可以设置本房间昵称。
7. 本房间昵称只在当前房间生效。

---

## 4.2 房间规则

### 房间状态

| 状态 | 说明 |
|---|---|
| `WAITING` | 等待玩家加入，尚未开始游戏 |
| `PLAYING` | 房间开启中，可以计分 |
| `CLOSED` | 房间已关闭，可以查看历史，可以由房主重新打开 |

### 状态流转

```text
创建房间
→ WAITING
→ 满员后房主点击开始
→ PLAYING
→ 房主关闭或系统自动关闭
→ CLOSED
→ 房主重新打开
→ PLAYING
```

### 房间重新打开

- 房主可以重新打开 CLOSED 房间。
- 重新打开后：
  - 创建新的 `room_session`。
  - 房间状态变为 PLAYING。
  - 继续原座位分数。
  - 继续原轮次编号。
  - 可以继续修改所有历史局。
  - 新产生的轮次属于新的 session。

---

## 4.3 房间 Session 规则

由于房间可以反复关闭和重新打开，需要记录每次开启时间段。

`room_session` 用于记录：

- 第几次开启。
- 开启时间。
- 关闭时间。
- 本 session 起始轮次。
- 本 session 结束轮次。
- 本 session 总分变化。
- 关闭原因：
  - `OWNER_CLOSE`
  - `AUTO_CLOSE`

---

## 4.4 座位规则

1. 每个房间最多 4 个座位。
2. 座位固定为：
   - 东：`EAST`
   - 南：`SOUTH`
   - 西：`WEST`
   - 北：`NORTH`
3. 2 人房只启用东、南。
4. 3 人房启用东、南、西。
5. 4 人房启用东、南、西、北。
6. 分数绑定在座位上。
7. 玩家退出或被踢后，座位变为空位，但座位分数保留。
8. 新玩家加入空座位时继承座位当前分数。
9. 新玩家个人战绩从加入后的下一轮开始统计。

---

## 4.5 计分规则

每个玩家提交的是“支出记录”，即：

```text
我输给谁多少分
```

例子：

| 玩家 | 提交内容 |
|---|---|
| 东 张三 | 输给南 10 分，输给西 5 分 |
| 南 李四 | 不输不赢 |
| 西 王五 | 输给南 3 分 |
| 北 赵六 | 输给东 8 分 |

系统计算：

| 玩家 | 本轮净分 |
|---|---:|
| 东 张三 | -10 -5 +8 = -7 |
| 南 李四 | +10 +3 = +13 |
| 西 王五 | +5 -3 = +2 |
| 北 赵六 | -8 |

### 校验规则

- 分数必须为正整数。
- 不允许输给自己。
- 一名玩家可以输给多个玩家。
- 同一个输家对同一个赢家可以合并为一条记录。
- 空提交表示不输不赢。
- 所有参与本轮的玩家提交后自动结算。
- 系统结算后自动创建下一轮。

---

## 4.6 历史局修改规则

房主可以修改、删除、撤销历史局。

### 撤销上一局

- 删除最近一局。
- 回滚该局对座位分和个人分的影响。
- 当前轮号回退。

### 修改某一局

- 修改该局输分明细。
- 从该局开始重算所有后续局：
  - 座位分。
  - 用户参与分。
  - 当前房间排行榜。
  - 用户个人战绩。
  - 历史总榜。
  - 月度榜。
  - session 统计。

### 删除某一局

- 删除该局。
- 后续轮次可以选择保留原轮号或重新排序。
- MVP 建议逻辑删除该局，保留原轮号，避免日志和引用混乱。
- 展示时隐藏已删除轮次。
- 重算后续数据。

---

## 5. 页面结构

## 5.1 页面清单

| 页面 | 路径建议 | 功能 |
|---|---|---|
| 启动页 | `/pages/splash/index` | 检查登录和手机号绑定 |
| 手机号绑定页 | `/pages/auth/bind-phone` | 绑定手机号 |
| 首页 | `/pages/home/index` | 创建房间、加入房间、最近房间、排行榜、规则入口 |
| 创建房间页 | `/pages/room/create` | 设置人数、初始分 |
| 加入房间页 | `/pages/room/join` | 输入房间号、扫码、分享进入 |
| 座位选择页 | `/pages/room/select-seat` | 选择空座位 |
| 房间等待页 | `/pages/room/waiting` | 座位列表、开始游戏、踢人、转让房主 |
| 房间计分页 | `/pages/room/play` | 当前轮计分、提交状态、实时明细 |
| 本轮提交编辑页 | `/pages/round/submit` | 添加输分记录 |
| 上一轮摘要弹窗 | 组件 | 自动结算后显示 |
| 历史局列表页 | `/pages/round/history` | 查看所有局 |
| 历史局详情页 | `/pages/round/detail` | 查看、修改、删除 |
| 房间排行榜页 | `/pages/room/rank` | 当前房间排行榜 |
| 个人战绩页 | `/pages/stats/my` | 个人统计 |
| 房间详情页 | `/pages/stats/room-detail` | 某个房间内个人战绩 |
| 规则页 | `/pages/rules/index` | 静态规则占位 |
| 个人资料页 | `/pages/user/profile` | 修改全局昵称 |

---

## 5.2 首页内容

首页展示：

- 用户头像、昵称。
- 创建房间按钮。
- 加入房间按钮。
- 最近房间列表。
- 历史总榜 Top 10。
- 月度榜 Top 10。
- 麻将规则入口。

---

## 5.3 房间计分页布局

房间计分页建议展示：

1. 房间名称、房间号、当前轮数。
2. 房主标识。
3. 当前房间状态。
4. 当前座位和玩家信息。
5. 当前房间排行榜。
6. 当前轮提交状态。
7. 当前轮所有提交内容。
8. 我的提交区域。
9. 房主操作区：
   - 代提交。
   - 强制结算。
   - 关闭房间。
   - 转让房主。
   - 踢人。
10. 历史局入口。

---

## 6. 数据库设计

> 命名风格：小写下划线。  
> 时间字段统一使用 `datetime`。  
> 金额/分数字段统一使用 `int`。  
> 所有核心表保留 `created_at`、`updated_at`。  
> 删除历史建议使用逻辑删除字段 `deleted`。

---

## 6.1 用户表 `sys_user`

```sql
CREATE TABLE sys_user (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    openid VARCHAR(128) NOT NULL COMMENT '微信openid',
    phone VARCHAR(32) NOT NULL COMMENT '绑定手机号，第一版不可换绑',
    nickname VARCHAR(64) DEFAULT NULL COMMENT '全局昵称',
    avatar_url VARCHAR(512) DEFAULT NULL COMMENT '微信头像',
    total_score INT NOT NULL DEFAULT 0 COMMENT '累计净分，冗余统计字段',
    total_rounds INT NOT NULL DEFAULT 0 COMMENT '累计参与轮数，冗余统计字段',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_openid (openid),
    UNIQUE KEY uk_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

---

## 6.2 房间表 `room`

```sql
CREATE TABLE room (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '房间ID',
    room_code VARCHAR(16) NOT NULL COMMENT '6位数字房间号',
    room_name VARCHAR(64) NOT NULL COMMENT '房间名称，默认麻将房间+房间号',
    owner_user_id BIGINT NOT NULL COMMENT '房主用户ID',
    player_count TINYINT NOT NULL COMMENT '房间人数：2-4',
    initial_score INT NOT NULL DEFAULT 0 COMMENT '初始分',
    status VARCHAR(16) NOT NULL COMMENT 'WAITING/PLAYING/CLOSED',
    current_round_no INT NOT NULL DEFAULT 0 COMMENT '已完成的最大轮次号',
    last_score_time DATETIME DEFAULT NULL COMMENT '最后一轮成功结算时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_room_code (room_code),
    KEY idx_owner_user_id (owner_user_id),
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='房间表';
```

---

## 6.3 房间开启段表 `room_session`

```sql
CREATE TABLE room_session (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '房间开启段ID',
    room_id BIGINT NOT NULL COMMENT '房间ID',
    session_no INT NOT NULL COMMENT '第几次开启',
    start_time DATETIME NOT NULL COMMENT '开启时间',
    end_time DATETIME DEFAULT NULL COMMENT '关闭时间',
    start_round_no INT NOT NULL DEFAULT 0 COMMENT '开启时已完成轮次',
    end_round_no INT DEFAULT NULL COMMENT '关闭时已完成轮次',
    close_reason VARCHAR(32) DEFAULT NULL COMMENT 'OWNER_CLOSE/AUTO_CLOSE',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_room_id (room_id),
    KEY idx_room_session (room_id, session_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='房间开启时间段表';
```

---

## 6.4 房间座位表 `room_seat`

```sql
CREATE TABLE room_seat (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '座位ID',
    room_id BIGINT NOT NULL COMMENT '房间ID',
    seat_no TINYINT NOT NULL COMMENT '座位序号：1-4',
    seat_name VARCHAR(16) NOT NULL COMMENT 'EAST/SOUTH/WEST/NORTH',
    current_user_id BIGINT DEFAULT NULL COMMENT '当前座位用户，为空表示空位',
    current_score INT NOT NULL DEFAULT 0 COMMENT '当前座位分数，用于继承',
    joined_at DATETIME DEFAULT NULL COMMENT '当前用户加入该座位时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_room_seat (room_id, seat_no),
    KEY idx_room_id (room_id),
    KEY idx_current_user_id (current_user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='房间座位表';
```

---

## 6.5 座位用户历史表 `room_seat_user_history`

```sql
CREATE TABLE room_seat_user_history (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '座位用户历史ID',
    room_id BIGINT NOT NULL COMMENT '房间ID',
    seat_id BIGINT NOT NULL COMMENT '座位ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    room_nickname VARCHAR(64) DEFAULT NULL COMMENT '本房间昵称',
    join_round_no INT NOT NULL COMMENT '从哪一轮之后开始统计，通常为当前已完成轮次+1',
    leave_round_no INT DEFAULT NULL COMMENT '离开时最后参与轮次',
    join_time DATETIME NOT NULL COMMENT '加入时间',
    leave_time DATETIME DEFAULT NULL COMMENT '离开时间',
    leave_reason VARCHAR(32) DEFAULT NULL COMMENT 'QUIT/KICKED/OWNER_TRANSFER_QUIT',
    active TINYINT NOT NULL DEFAULT 1 COMMENT '是否当前仍在座位',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_room_id (room_id),
    KEY idx_user_id (user_id),
    KEY idx_seat_id (seat_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='座位用户历史表';
```

---

## 6.6 轮次表 `round`

```sql
CREATE TABLE round (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '轮次ID',
    room_id BIGINT NOT NULL COMMENT '房间ID',
    session_id BIGINT NOT NULL COMMENT '所属开启段ID',
    round_no INT NOT NULL COMMENT '轮次编号，从1递增',
    status VARCHAR(16) NOT NULL COMMENT 'SUBMITTING/SETTLED/DELETED',
    settled_at DATETIME DEFAULT NULL COMMENT '结算时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_room_round (room_id, round_no),
    KEY idx_room_id (room_id),
    KEY idx_session_id (session_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='轮次表';
```

> 注意：`round` 是 MySQL 关键字风险较高，实际建表可命名为 `game_round`。  
> 推荐实际使用 `game_round`。

---

## 6.7 轮次参与者表 `round_participant`

```sql
CREATE TABLE round_participant (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '轮次参与者ID',
    room_id BIGINT NOT NULL,
    round_id BIGINT NOT NULL,
    seat_id BIGINT NOT NULL,
    user_id BIGINT DEFAULT NULL COMMENT '该轮对应用户，退出者当前轮仍可保留',
    submit_status VARCHAR(32) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING/SUBMITTED/OWNER_SUBMITTED/FORCED_SUBMITTED',
    submitted_by BIGINT DEFAULT NULL COMMENT '提交人用户ID，代提交时为房主ID',
    submitted_at DATETIME DEFAULT NULL,
    net_score INT NOT NULL DEFAULT 0 COMMENT '本轮净分',
    active_for_stats TINYINT NOT NULL DEFAULT 1 COMMENT '是否计入个人战绩',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_round_seat (round_id, seat_id),
    KEY idx_round_id (round_id),
    KEY idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='轮次参与者表';
```

---

## 6.8 输分记录表 `score_payment`

```sql
CREATE TABLE score_payment (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '输分记录ID',
    room_id BIGINT NOT NULL,
    round_id BIGINT NOT NULL,
    from_user_id BIGINT NOT NULL COMMENT '输钱用户',
    from_seat_id BIGINT NOT NULL COMMENT '输钱座位',
    to_user_id BIGINT NOT NULL COMMENT '收钱用户',
    to_seat_id BIGINT NOT NULL COMMENT '收钱座位',
    score INT NOT NULL COMMENT '输分，正整数',
    remark VARCHAR(255) DEFAULT NULL COMMENT '可选备注',
    created_by BIGINT NOT NULL COMMENT '创建人，自己提交或房主代提交',
    is_owner_submit TINYINT NOT NULL DEFAULT 0 COMMENT '是否房主代提交',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    KEY idx_round_id (round_id),
    KEY idx_from_user_id (from_user_id),
    KEY idx_to_user_id (to_user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='输分记录表';
```

---

## 6.9 用户房间统计表 `user_room_stats`

```sql
CREATE TABLE user_room_stats (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户房间统计ID',
    room_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    total_score INT NOT NULL DEFAULT 0 COMMENT '该用户在该房间实际参与净分',
    total_rounds INT NOT NULL DEFAULT 0 COMMENT '实际参与轮数',
    win_rounds INT NOT NULL DEFAULT 0 COMMENT '净分>0的轮数',
    lose_rounds INT NOT NULL DEFAULT 0 COMMENT '净分<0的轮数',
    draw_rounds INT NOT NULL DEFAULT 0 COMMENT '净分=0的轮数',
    positive_score_sum INT NOT NULL DEFAULT 0 COMMENT '盈利轮次总赢分',
    negative_score_sum INT NOT NULL DEFAULT 0 COMMENT '亏损轮次总输分绝对值',
    highest_round_score INT DEFAULT NULL COMMENT '最高单轮得分',
    lowest_round_score INT DEFAULT NULL COMMENT '最低单轮得分',
    first_join_time DATETIME DEFAULT NULL,
    last_play_time DATETIME DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_room_user (room_id, user_id),
    KEY idx_user_id (user_id),
    KEY idx_room_id (room_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户房间统计表';
```

---

## 6.10 用户每日统计表 `user_daily_stats`

用于快速计算本周、本月、月度榜。

```sql
CREATE TABLE user_daily_stats (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户每日统计ID',
    user_id BIGINT NOT NULL,
    stat_date DATE NOT NULL COMMENT '统计日期',
    total_score INT NOT NULL DEFAULT 0,
    total_rounds INT NOT NULL DEFAULT 0,
    win_rounds INT NOT NULL DEFAULT 0,
    positive_score_sum INT NOT NULL DEFAULT 0,
    negative_score_sum INT NOT NULL DEFAULT 0,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_date (user_id, stat_date),
    KEY idx_stat_date (stat_date),
    KEY idx_user_id (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户每日统计表';
```

---

## 6.11 操作日志表 `room_operation_log`

```sql
CREATE TABLE room_operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '操作日志ID',
    room_id BIGINT NOT NULL,
    operator_user_id BIGINT NOT NULL COMMENT '操作人',
    operation_type VARCHAR(64) NOT NULL COMMENT '操作类型',
    target_user_id BIGINT DEFAULT NULL COMMENT '目标用户',
    target_round_id BIGINT DEFAULT NULL COMMENT '目标轮次',
    before_data JSON DEFAULT NULL COMMENT '操作前数据',
    after_data JSON DEFAULT NULL COMMENT '操作后数据',
    remark VARCHAR(255) DEFAULT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    KEY idx_room_id (room_id),
    KEY idx_operator_user_id (operator_user_id),
    KEY idx_operation_type (operation_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='房间操作日志表';
```

---

## 7. 后端接口设计

统一返回格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

错误示例：

```json
{
  "code": 40001,
  "message": "房间不存在",
  "data": null
}
```

---

## 7.1 认证接口

### 微信登录

`POST /api/auth/wechat-login`

请求：

```json
{
  "code": "wx_login_code"
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "token": "jwt-token",
    "userId": 10001,
    "hasBindPhone": false,
    "nickname": "微信用户",
    "avatarUrl": "https://..."
  }
}
```

---

### 绑定手机号

`POST /api/auth/bind-phone`

请求：

```json
{
  "phoneCode": "getPhoneNumber_return_code"
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "phone": "138****8888"
  }
}
```

规则：

- 未绑定手机号不能进入首页。
- 已绑定手机号再次调用返回错误。
- 第一版不支持换绑。

---

### 获取当前用户

`GET /api/user/me`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userId": 10001,
    "phone": "138****8888",
    "nickname": "老李",
    "avatarUrl": "https://..."
  }
}
```

---

### 修改全局昵称

`PUT /api/user/profile`

请求：

```json
{
  "nickname": "老李"
}
```

---

## 7.2 房间接口

### 创建房间

`POST /api/rooms`

请求：

```json
{
  "playerCount": 4,
  "initialScore": 0
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roomId": 20001,
    "roomCode": "836521",
    "roomName": "麻将房间 836521",
    "status": "WAITING",
    "ownerUserId": 10001
  }
}
```

创建逻辑：

- 生成 6 位房间号。
- 创建 room。
- 创建 2-4 个 seat。
- 房主默认进入东位，或进入座位选择页由房主选择。
- 推荐 MVP：房主默认东位。

---

### 获取房间快照

`GET /api/rooms/{roomId}/snapshot`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roomId": 20001,
    "roomCode": "836521",
    "roomName": "麻将房间 836521",
    "status": "PLAYING",
    "ownerUserId": 10001,
    "playerCount": 4,
    "currentRoundNo": 8,
    "currentSubmittingRoundNo": 9,
    "seats": [
      {
        "seatId": 1,
        "seatName": "EAST",
        "displayName": "东",
        "currentUserId": 10001,
        "nickname": "老李",
        "roomNickname": "东风",
        "avatarUrl": "https://...",
        "currentScore": 30,
        "empty": false
      }
    ],
    "currentRound": {
      "roundId": 90001,
      "roundNo": 9,
      "status": "SUBMITTING",
      "participants": [
        {
          "seatId": 1,
          "userId": 10001,
          "nickname": "老李",
          "submitStatus": "SUBMITTED",
          "payments": [
            {
              "toUserId": 10002,
              "toSeatName": "SOUTH",
              "score": 10,
              "remark": "点炮"
            }
          ]
        }
      ]
    },
    "rankList": [
      {
        "rank": 1,
        "userId": 10001,
        "nickname": "老李",
        "avatarUrl": "https://...",
        "score": 50,
        "rounds": 8
      }
    ]
  }
}
```

---

### 通过房间号查询

`GET /api/rooms/code/{roomCode}`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roomId": 20001,
    "roomCode": "836521",
    "roomName": "麻将房间 836521",
    "status": "WAITING",
    "emptySeats": [
      {
        "seatId": 2,
        "seatName": "SOUTH",
        "displayName": "南",
        "currentScore": 0
      }
    ]
  }
}
```

---

### 加入房间座位

`POST /api/rooms/{roomId}/join`

请求：

```json
{
  "seatId": 2,
  "roomNickname": "小王"
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roomId": 20001,
    "seatId": 2,
    "seatName": "SOUTH",
    "inheritedScore": -20
  }
}
```

---

### 开始游戏

`POST /api/rooms/{roomId}/start`

规则：

- 仅房主可调用。
- 房间必须为 WAITING。
- 必须满员。
- 创建 room_session。
- 创建第 1 个提交中 round。
- 状态变为 PLAYING。

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roomId": 20001,
    "status": "PLAYING",
    "roundNo": 1
  }
}
```

---

### 关闭房间

`POST /api/rooms/{roomId}/close`

请求：

```json
{
  "reason": "OWNER_CLOSE"
}
```

规则：

- 仅房主可调用。
- 当前 session 结束。
- room 状态变为 CLOSED。
- 保留座位分、历史局、统计。

---

### 重新打开房间

`POST /api/rooms/{roomId}/reopen`

规则：

- 仅房主可调用。
- room 状态必须为 CLOSED。
- 创建新的 room_session。
- 继续原轮次。
- 创建新的提交中 round。
- 状态变为 PLAYING。

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roomId": 20001,
    "status": "PLAYING",
    "currentRoundNo": 20,
    "currentSubmittingRoundNo": 21
  }
}
```

---

### 转让房主

`POST /api/rooms/{roomId}/transfer-owner`

请求：

```json
{
  "targetUserId": 10002
}
```

规则：

- 仅房主可调用。
- 目标用户必须是当前房间内玩家。

---

### 踢人

`POST /api/rooms/{roomId}/kick`

请求：

```json
{
  "targetUserId": 10003
}
```

规则：

- 仅房主可调用。
- 被踢用户所在座位变为空位。
- 座位分保留。
- 被踢用户个人统计周期结束。
- 如果被踢用户参与当前轮，当前轮仍保留其参与者记录，本轮默认不输钱。

---

### 主动退出

`POST /api/rooms/{roomId}/quit`

规则：

- 玩家可主动退出。
- 座位变为空位。
- 座位分保留。
- 如果房主退出：
  - 自动转让给当前房间内最早加入的其他玩家。
  - 若没有其他玩家，则房间关闭，原房主仍作为 room.owner_user_id。

---

## 7.3 轮次与计分接口

### 提交本轮输分

`POST /api/rounds/{roundId}/submit`

请求：

```json
{
  "payments": [
    {
      "toUserId": 10002,
      "score": 10,
      "remark": "点炮"
    },
    {
      "toUserId": 10003,
      "score": 5,
      "remark": ""
    }
  ]
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roundId": 90001,
    "submitStatus": "SUBMITTED",
    "allSubmitted": false
  }
}
```

规则：

- 普通玩家只能提交自己的记录。
- 如果本轮未结算，可以重复提交，后一次覆盖自己的提交。
- 如果所有参与者都提交，系统自动结算并创建下一轮。
- 结算结果通过 WebSocket 广播。

---

### 房主代提交

`POST /api/rounds/{roundId}/owner-submit`

请求：

```json
{
  "targetUserId": 10003,
  "payments": [
    {
      "toUserId": 10001,
      "score": 8,
      "remark": "房主代填"
    }
  ]
}
```

规则：

- 仅房主可调用。
- 只能代提交未提交玩家。
- 不能覆盖已提交内容。
- 明细页显示“由房主代提交”。

---

### 房主强制不输不赢

`POST /api/rounds/{roundId}/force-neutral`

请求：

```json
{
  "targetUserId": 10003
}
```

规则：

- 仅房主可调用。
- 目标玩家必须未提交。
- 设置为空提交。
- 状态为 `FORCED_SUBMITTED`。
- 若因此所有人提交，则自动结算。

---

### 获取历史局列表

`GET /api/rooms/{roomId}/rounds`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "roundId": 90001,
      "roundNo": 1,
      "settledAt": "2026-04-29 20:10:00",
      "summary": [
        {
          "userId": 10001,
          "nickname": "老李",
          "netScore": 10
        },
        {
          "userId": 10002,
          "nickname": "小王",
          "netScore": -10
        }
      ]
    }
  ]
}
```

---

### 获取历史局详情

`GET /api/rounds/{roundId}`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "roundId": 90001,
    "roundNo": 1,
    "status": "SETTLED",
    "payments": [
      {
        "fromUserId": 10002,
        "fromNickname": "小王",
        "toUserId": 10001,
        "toNickname": "老李",
        "score": 10,
        "remark": "点炮",
        "isOwnerSubmit": false
      }
    ],
    "summary": [
      {
        "userId": 10001,
        "netScore": 10
      },
      {
        "userId": 10002,
        "netScore": -10
      }
    ]
  }
}
```

---

### 修改历史局

`PUT /api/rounds/{roundId}`

请求：

```json
{
  "payments": [
    {
      "fromUserId": 10002,
      "toUserId": 10001,
      "score": 20,
      "remark": "修正分数"
    }
  ]
}
```

规则：

- 仅房主可调用。
- 房间必须不是 WAITING。
- 修改后从该局开始重算全部后续局。
- 记录操作日志。
- 广播 `ROUND_RECALCULATED`。

---

### 删除历史局

`DELETE /api/rounds/{roundId}`

规则：

- 仅房主可调用。
- 逻辑删除。
- 从该局开始重算后续局。
- 记录操作日志。
- 广播 `ROUND_RECALCULATED`。

---

### 撤销上一局

`POST /api/rooms/{roomId}/rounds/undo-last`

规则：

- 仅房主可调用。
- 删除最近一局已结算记录。
- 回滚分数。
- 记录操作日志。
- 广播 `ROUND_RECALCULATED`。

---

## 7.4 统计接口

### 当前房间排行榜

`GET /api/rooms/{roomId}/rank`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "rank": 1,
      "userId": 10001,
      "nickname": "老李",
      "avatarUrl": "https://...",
      "score": 58,
      "rounds": 12
    }
  ]
}
```

排序规则：

- 当前在座玩家。
- 按该用户在当前房间的个人参与净分降序。
- 不展示座位分。

---

### 首页排行榜

`GET /api/rankings/home`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalRank": [
      {
        "rank": 1,
        "userId": 10001,
        "nickname": "老李",
        "avatarUrl": "https://...",
        "score": 980
      }
    ],
    "monthlyRank": [
      {
        "rank": 1,
        "userId": 10002,
        "nickname": "小王",
        "avatarUrl": "https://...",
        "score": 180
      }
    ]
  }
}
```

---

### 个人战绩

`GET /api/stats/me?range=ALL`

`range` 可选：

- `ALL`
- `MONTH`
- `WEEK`

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalRooms": 12,
    "totalRounds": 180,
    "totalScore": 560,
    "avgRoundScore": 3.11,
    "highestRoundScore": 50,
    "lowestRoundScore": -40,
    "roundWinRate": 0.43,
    "roomWinRate": 0.25,
    "profitLossRatio": 1.35,
    "recentRooms": [
      {
        "roomId": 20001,
        "roomName": "麻将房间 836521",
        "status": "CLOSED",
        "myScore": 80,
        "myRank": 1,
        "rounds": 20,
        "lastPlayTime": "2026-04-29 23:00:00"
      }
    ]
  }
}
```

---

## 8. WebSocket 消息协议

## 8.1 连接地址

```text
wss://your-domain.com/ws/room?token={jwt}&roomId={roomId}
```

连接成功后，后端将用户加入对应 room channel。

---

## 8.2 客户端发送消息格式

```json
{
  "type": "PING",
  "roomId": 20001,
  "payload": {}
}
```

常用客户端消息：

| type | 说明 |
|---|---|
| `PING` | 心跳 |
| `SUBSCRIBE_ROOM` | 订阅房间 |
| `UNSUBSCRIBE_ROOM` | 取消订阅 |

多数业务操作通过 REST API 完成，WebSocket 主要负责推送状态变化。

---

## 8.3 服务端推送通用格式

```json
{
  "type": "ROOM_UPDATED",
  "roomId": 20001,
  "timestamp": "2026-04-29 20:00:00",
  "payload": {}
}
```

---

## 8.4 事件类型

| 事件 | 说明 |
|---|---|
| `ROOM_UPDATED` | 房间基础信息变化 |
| `SEAT_UPDATED` | 加入、退出、被踢、换人 |
| `OWNER_CHANGED` | 房主变化 |
| `GAME_STARTED` | 游戏开始 |
| `ROUND_SUBMITTED` | 某玩家提交 |
| `ROUND_SUBMIT_MODIFIED` | 某玩家修改提交 |
| `ROUND_SETTLED` | 本轮结算 |
| `ROUND_RECALCULATED` | 历史局修改后重算 |
| `ROOM_CLOSED` | 房间关闭 |
| `ROOM_REOPENED` | 房间重新打开 |
| `USER_KICKED` | 当前用户被踢 |
| `ERROR_NOTICE` | 错误提醒 |

---

## 8.5 示例：玩家提交

```json
{
  "type": "ROUND_SUBMITTED",
  "roomId": 20001,
  "timestamp": "2026-04-29 20:01:00",
  "payload": {
    "roundId": 90001,
    "roundNo": 9,
    "userId": 10001,
    "nickname": "老李",
    "submitStatus": "SUBMITTED",
    "payments": [
      {
        "toUserId": 10002,
        "toNickname": "小王",
        "score": 10,
        "remark": "点炮"
      }
    ]
  }
}
```

---

## 8.6 示例：玩家修改提交

```json
{
  "type": "ROUND_SUBMIT_MODIFIED",
  "roomId": 20001,
  "timestamp": "2026-04-29 20:02:00",
  "payload": {
    "roundId": 90001,
    "userId": 10001,
    "nickname": "老李",
    "message": "老李修改了本轮提交"
  }
}
```

---

## 8.7 示例：本轮结算

```json
{
  "type": "ROUND_SETTLED",
  "roomId": 20001,
  "timestamp": "2026-04-29 20:05:00",
  "payload": {
    "settledRound": {
      "roundId": 90001,
      "roundNo": 9,
      "summary": [
        {
          "userId": 10001,
          "nickname": "老李",
          "netScore": 15
        },
        {
          "userId": 10002,
          "nickname": "小王",
          "netScore": -15
        }
      ]
    },
    "nextRound": {
      "roundId": 90002,
      "roundNo": 10,
      "status": "SUBMITTING"
    }
  }
}
```

---

## 8.8 示例：历史重算

```json
{
  "type": "ROUND_RECALCULATED",
  "roomId": 20001,
  "timestamp": "2026-04-29 20:10:00",
  "payload": {
    "fromRoundNo": 3,
    "message": "房主修改了第3局，系统已重算后续分数"
  }
}
```

---

## 9. 统计指标计算方式

## 9.1 单轮净分

```text
某用户本轮净分 = 收到的分数总和 - 支出的分数总和
```

---

## 9.2 用户总净分

```text
用户总净分 = 所有实际参与轮次的净分之和
```

---

## 9.3 平均每轮得分

```text
平均每轮得分 = 总净分 / 实际参与轮数
```

---

## 9.4 单轮胜率

```text
单轮胜率 = 净分 > 0 的轮次数 / 实际参与总轮数
```

---

## 9.5 房间胜率

```text
房间胜率 = 用户个人参与分排名第1的房间数 / 用户参与过的房间数
```

如果多人并列第一，则并列第一者都算房间胜利。

---

## 9.6 盈亏比

```text
盈亏比 = 盈利轮次总赢分 / 亏损轮次总输分绝对值
```

例子：

```text
盈利轮次总赢分 = 120
亏损轮次总输分绝对值 = 80
盈亏比 = 1.5
```

特殊情况：

- 如果亏损轮次总输分为 0：
  - 如果盈利分也为 0，显示 `--`。
  - 如果盈利分大于 0，显示 `∞`。

---

## 10. 后端目录结构建议

```text
mahjong-score-server
├── pom.xml
├── src/main/java/com/example/mahjong
│   ├── MahjongApplication.java
│   ├── common
│   │   ├── api
│   │   │   ├── ApiResponse.java
│   │   │   └── ErrorCode.java
│   │   ├── exception
│   │   │   ├── BizException.java
│   │   │   └── GlobalExceptionHandler.java
│   │   ├── security
│   │   │   ├── JwtUtil.java
│   │   │   ├── JwtAuthFilter.java
│   │   │   └── UserContext.java
│   │   └── config
│   │       ├── WebMvcConfig.java
│   │       ├── MybatisPlusConfig.java
│   │       ├── SwaggerConfig.java
│   │       ├── WebSocketConfig.java
│   │       └── ScheduleConfig.java
│   ├── module
│   │   ├── auth
│   │   │   ├── controller
│   │   │   ├── service
│   │   │   └── dto
│   │   ├── user
│   │   ├── room
│   │   ├── round
│   │   ├── score
│   │   ├── stats
│   │   └── ranking
│   ├── websocket
│   │   ├── RoomWebSocketHandler.java
│   │   ├── RoomWebSocketSessionManager.java
│   │   ├── WsMessage.java
│   │   └── WsMessageType.java
│   └── task
│       └── RoomAutoCloseTask.java
└── src/main/resources
    ├── application.yml
    ├── mapper
    │   ├── RoomMapper.xml
    │   ├── RoundMapper.xml
    │   └── StatsMapper.xml
    └── db
        └── schema.sql
```

---

## 11. 前端目录结构建议

```text
mahjong-score-miniprogram
├── package.json
├── pages.json
├── manifest.json
├── App.vue
├── main.js
├── src
│   ├── pages
│   │   ├── splash
│   │   ├── auth
│   │   │   └── bind-phone.vue
│   │   ├── home
│   │   │   └── index.vue
│   │   ├── room
│   │   │   ├── create.vue
│   │   │   ├── join.vue
│   │   │   ├── select-seat.vue
│   │   │   ├── waiting.vue
│   │   │   ├── play.vue
│   │   │   └── rank.vue
│   │   ├── round
│   │   │   ├── submit.vue
│   │   │   ├── history.vue
│   │   │   └── detail.vue
│   │   ├── stats
│   │   │   ├── my.vue
│   │   │   └── room-detail.vue
│   │   ├── rules
│   │   │   └── index.vue
│   │   └── user
│   │       └── profile.vue
│   ├── components
│   │   ├── BaseCard.vue
│   │   ├── BaseButton.vue
│   │   ├── SeatCard.vue
│   │   ├── ScoreInputRow.vue
│   │   ├── RoundSummaryPopup.vue
│   │   └── RankList.vue
│   ├── stores
│   │   ├── user.js
│   │   ├── room.js
│   │   └── socket.js
│   ├── api
│   │   ├── auth.js
│   │   ├── room.js
│   │   ├── round.js
│   │   ├── stats.js
│   │   └── ranking.js
│   ├── utils
│   │   ├── request.js
│   │   ├── socket.js
│   │   ├── auth.js
│   │   └── format.js
│   └── styles
│       ├── variables.scss
│       └── common.scss
```

---

## 12. 关键业务伪代码

## 12.1 提交本轮输分

```java
@Transactional
public SubmitResult submitRound(Long roundId, Long userId, List<PaymentDTO> payments) {
    Round round = getRound(roundId);
    assertRoomPlaying(round.getRoomId());
    assertRoundSubmitting(round);

    RoundParticipant participant = getParticipant(roundId, userId);
    if (participant == null) {
        throw new BizException("你不是本轮参与者");
    }

    if ("SETTLED".equals(round.getStatus())) {
        throw new BizException("本轮已结算，不能修改");
    }

    validatePayments(userId, payments);

    deleteOldPayments(roundId, userId);
    savePayments(roundId, userId, payments, false);

    participant.setSubmitStatus("SUBMITTED");
    participant.setSubmittedBy(userId);
    participant.setSubmittedAt(now());
    updateParticipant(participant);

    broadcastRoundSubmitted(roundId, userId);

    if (allParticipantsSubmitted(roundId)) {
        settleRound(roundId);
    }

    return result;
}
```

---

## 12.2 结算轮次

```java
@Transactional
public void settleRound(Long roundId) {
    Round round = getRoundForUpdate(roundId);

    if (!"SUBMITTING".equals(round.getStatus())) {
        return;
    }

    List<RoundParticipant> participants = listParticipants(roundId);
    List<ScorePayment> payments = listPayments(roundId);

    Map<Long, Integer> userNetScore = new HashMap<>();
    Map<Long, Integer> seatNetScore = new HashMap<>();

    for (ScorePayment p : payments) {
        userNetScore.merge(p.getFromUserId(), -p.getScore(), Integer::sum);
        userNetScore.merge(p.getToUserId(), p.getScore(), Integer::sum);

        seatNetScore.merge(p.getFromSeatId(), -p.getScore(), Integer::sum);
        seatNetScore.merge(p.getToSeatId(), p.getScore(), Integer::sum);
    }

    updateRoundParticipantsNetScore(participants, userNetScore);
    updateSeatScores(seatNetScore);
    updateUserRoomStats(round.getRoomId(), roundId);
    updateUserDailyStats(roundId);

    round.setStatus("SETTLED");
    round.setSettledAt(now());
    updateRound(round);

    Room room = getRoom(round.getRoomId());
    room.setCurrentRoundNo(round.getRoundNo());
    room.setLastScoreTime(now());
    updateRoom(room);

    createNextSubmittingRound(room);

    broadcastRoundSettled(roundId);
}
```

---

## 12.3 历史局重算

```java
@Transactional
public void recalculateFromRound(Long roomId, Integer fromRoundNo) {
    Room room = getRoomForUpdate(roomId);

    resetSeatScoresToInitial(roomId);
    resetAllUserRoomStats(roomId);
    resetDailyStatsRelatedToRoom(roomId);

    List<Round> rounds = listSettledRounds(roomId);

    for (Round round : rounds) {
        if (round.isDeleted()) {
            continue;
        }

        List<ScorePayment> payments = listPayments(round.getId());
        applyRoundScore(round, payments);
    }

    refreshRoomCurrentRoundNo(roomId);
    refreshAllRankCacheIfNeeded(roomId);

    broadcastRoundRecalculated(roomId, fromRoundNo);
}
```

---

## 12.4 自动关闭房间

```java
@Scheduled(cron = "0 */5 * * * ?")
public void autoCloseInactiveRooms() {
    List<Room> playingRooms = roomMapper.selectPlayingRooms();

    for (Room room : playingRooms) {
        RoomSession session = getCurrentSession(room.getId());

        LocalDateTime baseTime = room.getLastScoreTime() != null
            ? room.getLastScoreTime()
            : session.getStartTime();

        if (Duration.between(baseTime, now()).toMinutes() >= 60) {
            closeRoom(room.getId(), "AUTO_CLOSE");
        }
    }
}
```

---

## 13. 权限控制规则

| 操作 | 房主 | 普通玩家 | 备注 |
|---|---|---|---|
| 创建房间 | 可以 | 可以 | 创建者成为房主 |
| 加入房间 | 可以 | 可以 | 房间未满且有空位 |
| 开始游戏 | 可以 | 不可以 | 必须满员 |
| 关闭房间 | 可以 | 不可以 | CLOSED 后可查看 |
| 重新打开房间 | 可以 | 不可以 | 仅房主 |
| 踢人 | 可以 | 不可以 | 座位分保留 |
| 转让房主 | 可以 | 不可以 | 目标必须在房间内 |
| 代提交 | 可以 | 不可以 | 只能代未提交玩家 |
| 强制不输不赢 | 可以 | 不可以 | 目标必须未提交 |
| 提交自己输分 | 可以 | 可以 | 结算前可修改 |
| 修改历史局 | 可以 | 不可以 | 修改后全量重算 |
| 删除历史局 | 可以 | 不可以 | 逻辑删除 |
| 撤销上一局 | 可以 | 不可以 | 回滚最近局 |
| 查看历史明细 | 可以 | 可以 | 参与玩家可看 |
| 查看规则 | 可以 | 可以 | 静态页面 |

---

## 14. 异常场景处理

### 14.1 玩家断线

- WebSocket 断开后前端自动重连。
- 重连成功后重新调用房间快照接口。
- 断线不等于退出房间。
- 提交状态仍保留。

### 14.2 玩家长时间不提交

- 房主可以代提交。
- 房主可以强制设置为不输不赢。
- 如果无人操作，房间可能因为 1 小时无成功结算而自动关闭。

### 14.3 房间未满

- WAITING 状态下不能开始游戏。
- 创建时选择几人房，就必须对应座位都有人。

### 14.4 修改历史导致分数变化

- 所有历史局修改必须记录日志。
- 修改后自动重算后续分数。
- WebSocket 广播给房间内在线用户。
- 前端收到后重新拉取房间快照。

### 14.5 房主退出

- 如果房间内还有其他玩家，自动转让给最早加入的玩家。
- 如果没有其他玩家，房间关闭，原房主仍保留为 owner，后续可重新打开。

### 14.6 新玩家加入进行中房间

- 新玩家只能选择空座位。
- 继承座位分。
- 不参与当前已经创建的提交中轮次。
- 从下一轮开始参与个人统计。

---

## 15. UI 风格规范

### 15.1 风格

- 简洁工具风。
- 蓝白主色。
- 信息清晰优先。
- 不做复杂国风装饰。
- 卡片式布局。
- 大按钮，适合线下快速操作。

### 15.2 色彩建议

```scss
$primary-color: #1677ff;
$primary-light: #e6f4ff;
$background-color: #f5f7fb;
$card-color: #ffffff;
$text-main: #1f2937;
$text-secondary: #6b7280;
$success-color: #16a34a;
$danger-color: #dc2626;
$warning-color: #f59e0b;
$border-color: #e5e7eb;
```

### 15.3 核心组件

- `BaseCard`
- `BaseButton`
- `SeatCard`
- `ScoreInputRow`
- `RankList`
- `RoundSummaryPopup`
- `SubmitStatusTag`

---

## 16. Codex 开发任务拆解

## 16.1 阶段一：后端基础工程

Prompt：

```text
请基于 Java 17、Spring Boot 3.x、Maven、MyBatis-Plus、MySQL 8 创建一个麻将计分小程序后端工程。
要求：
1. 创建统一返回格式 ApiResponse。
2. 创建全局异常 BizException 和 GlobalExceptionHandler。
3. 集成 MyBatis-Plus。
4. 集成 JWT 鉴权过滤器。
5. 集成 Knife4j/Swagger。
6. 创建 application.yml。
7. 按模块划分 auth、user、room、round、stats、ranking。
8. 暂时不实现业务，只搭建可运行框架。
```

---

## 16.2 阶段二：数据库和实体

Prompt：

```text
请根据项目文档中的数据库设计，为 Spring Boot 后端创建 MySQL 建表 SQL、Entity、Mapper、Service 基础类。
表包括：
sys_user、room、room_session、room_seat、room_seat_user_history、game_round、round_participant、score_payment、user_room_stats、user_daily_stats、room_operation_log。
要求：
1. 使用 MyBatis-Plus 注解。
2. 所有时间字段使用 LocalDateTime。
3. game_round 替代 round 作为实际表名。
4. 提供 mapper XML 或 BaseMapper。
5. 不要省略字段。
```

---

## 16.3 阶段三：登录与手机号绑定

Prompt：

```text
请实现微信小程序登录和手机号绑定模块。
要求：
1. POST /api/auth/wechat-login 接收 wx.login code。
2. 后端调用微信 jscode2session，获取 openid。
3. 如果用户不存在则创建用户。
4. 返回 JWT 和 hasBindPhone。
5. POST /api/auth/bind-phone 接收 getPhoneNumber 返回的 phoneCode。
6. 后端换取手机号并绑定到当前用户。
7. 手机号绑定后不允许换绑。
8. 未绑定手机号的用户访问核心接口时返回错误。
```

---

## 16.4 阶段四：房间基础功能

Prompt：

```text
请实现房间模块：
1. 创建房间，生成6位房间号。
2. 默认房间名为“麻将房间 + 房间号”。
3. 支持2-4人房。
4. 创建座位：东、南、西、北，按人数启用。
5. 房主默认进入东位。
6. 通过房间号查询房间。
7. 加入空座位，支持本房间昵称。
8. 多个空位由前端传 seatId。
9. 获取房间快照。
10. 实现开始游戏：必须满员，创建 room_session 和第一轮 game_round。
```

---

## 16.5 阶段五：WebSocket

Prompt：

```text
请实现 Spring WebSocket 房间实时同步：
1. WebSocket 地址 /ws/room?token={jwt}&roomId={roomId}。
2. 连接时校验 JWT。
3. 将 session 加入对应 roomId channel。
4. 提供 RoomWebSocketSessionManager。
5. 定义 WsMessage 和 WsMessageType。
6. 实现广播方法 broadcastToRoom(roomId, message)。
7. 支持 PING/PONG。
8. 用户断开时移除 session，但不退出房间。
```

---

## 16.6 阶段六：计分与结算

Prompt：

```text
请实现麻将计分核心逻辑：
1. 每轮每个玩家可以提交多条输分记录。
2. 输分记录包括 toUserId、score、remark。
3. 校验 score 为正整数，不能输给自己。
4. 玩家结算前可重复提交，后一次覆盖自己的提交。
5. 房主可代提交未提交玩家。
6. 房主可强制未提交玩家不输不赢。
7. 所有人提交后自动结算。
8. 结算时计算每个用户净分、每个座位净分。
9. 更新 room_seat.current_score。
10. 更新 user_room_stats 和 user_daily_stats。
11. 自动创建下一轮。
12. 通过 WebSocket 广播 ROUND_SETTLED。
```

---

## 16.7 阶段七：房主操作与历史重算

Prompt：

```text
请实现房主操作：
1. 踢人：座位变空，座位分保留。
2. 转让房主：目标必须是当前房间玩家。
3. 主动退出：普通玩家直接退出；房主退出时自动转让给最早加入玩家。
4. 关闭房间：结束当前 room_session，状态变 CLOSED。
5. 重新打开：创建新 room_session，状态变 PLAYING，继续原轮次。
6. 修改历史局。
7. 删除历史局，使用逻辑删除。
8. 撤销上一局。
9. 所有修改历史的操作都要从变更轮次开始重算后续分数。
10. 所有房主操作写入 room_operation_log。
```

---

## 16.8 阶段八：统计与排行榜

Prompt：

```text
请实现统计与排行榜：
1. 当前房间排行榜按当前在座玩家的个人参与净分排序，不展示座位分。
2. 首页历史总榜按用户累计净分取前10。
3. 首页月度榜按本月用户累计净分取前10。
4. 个人战绩支持 ALL/MONTH/WEEK。
5. 统计总房间数、总轮数、总净分、平均每轮得分、最高单轮、最低单轮。
6. 单轮胜率 = 净分>0轮数 / 实际参与轮数。
7. 房间胜率 = 个人参与分排名第1的房间数 / 参与房间数，并列第一都算赢。
8. 盈亏比 = 盈利轮次总赢分 / 亏损轮次总输分绝对值。
```

---

## 16.9 阶段九：前端基础工程

Prompt：

```text
请基于 uni-app + Vue 3 + Pinia 创建微信小程序前端工程。
要求：
1. 使用蓝白简洁工具风。
2. 创建 request.js，统一封装 HTTP 请求和 JWT。
3. 创建 socket.js，封装 SocketTask、自动重连、心跳、关闭连接。
4. 创建 user、room、socket 三个 Pinia store。
5. 创建 pages.json 页面路由。
6. 创建基础组件 BaseCard、BaseButton、SeatCard、RankList、RoundSummaryPopup。
7. 不依赖复杂第三方 UI 库。
```

---

## 16.10 阶段十：前端页面开发

Prompt：

```text
请实现麻将计分微信小程序前端页面：
1. 启动页：检查登录和手机号绑定。
2. 手机号绑定页。
3. 首页：创建房间、加入房间、最近房间、历史总榜、月度榜、规则入口。
4. 创建房间页。
5. 加入房间页。
6. 座位选择页。
7. 房间等待页。
8. 房间计分页。
9. 本轮提交页。
10. 历史局列表页。
11. 历史局详情页。
12. 当前房间排行榜页。
13. 个人战绩页。
14. 静态规则页。
要求：
- 房间页进入时拉取 snapshot 并建立 WebSocket。
- WebSocket 收到事件后更新 Pinia 状态。
- 收到 ROUND_SETTLED 后弹出上一轮摘要。
- 离开房间页后关闭 WebSocket。
```

---

## 17. 开发顺序建议

推荐顺序：

1. 后端基础工程。
2. 数据库建表和实体。
3. 登录与手机号绑定。
4. 房间创建、加入、座位。
5. 房间快照接口。
6. WebSocket 框架。
7. 计分提交和自动结算。
8. 房主操作。
9. 历史局修改和重算。
10. 统计与排行榜。
11. 前端基础工程。
12. 前端登录和首页。
13. 前端房间流程。
14. 前端计分流程。
15. 前端战绩和排行榜。
16. 联调。
17. 部署和微信小程序域名配置。

---

## 18. MVP 验收标准

MVP 完成后，应满足：

1. 新用户首次进入必须绑定手机号。
2. 用户可以创建 2-4 人房间。
3. 房间号为 6 位数字。
4. 玩家可以通过房间号加入。
5. 满员后房主可以开始游戏。
6. 房间内 WebSocket 实时同步。
7. 每个玩家可以提交自己输给谁多少分。
8. 每个人提交后自动结算。
9. 结算后自动进入下一轮并弹出摘要。
10. 房主可以代提交未提交玩家。
11. 房主可以踢人，座位分保留。
12. 新玩家可以加入空位并继承座位分。
13. 个人战绩只统计自己实际参与轮次。
14. 房主可以关闭并重新打开房间。
15. 房主可以修改历史局并触发重算。
16. 当前房间排行榜按个人参与分排序。
17. 首页展示历史总榜和月度榜 Top 10。
18. 个人战绩页展示胜率、盈亏比等指标。
19. 房间 1 小时无新结算自动关闭。
20. 规则页静态占位可访问。

---

## 19. 部署注意事项

### 后端

- 必须部署 HTTPS。
- WebSocket 必须使用 WSS。
- 微信小程序后台需要配置：
  - request 合法域名。
  - socket 合法域名。
  - uploadFile/downloadFile 合法域名如后续需要。
- 数据库使用 MySQL 8。
- 初期单实例部署即可。
- 后续多实例需要 Redis 或消息队列同步 WebSocket 广播。

### 前端

- 微信开发者工具中配置 AppID。
- manifest.json 配置微信小程序信息。
- 发布前检查：
  - 合法域名。
  - 隐私协议。
  - 手机号授权说明。
  - 用户信息展示合规。
  - 页面分享参数是否正确。

---

## 20. 风险与注意点

### 20.1 最大技术风险

历史局修改后全量重算是最容易出 bug 的地方。

建议：

- 所有计分只以 `score_payment` 原始流水为准。
- `room_seat.current_score`、`user_room_stats`、`user_daily_stats` 都是可重算结果。
- 不要只依赖冗余字段。
- 修改历史局后统一走重算方法。

### 20.2 最大产品风险

座位分和个人分容易混淆。

必须坚持：

- 座位分用于继承。
- 个人分用于排行榜和个人战绩。
- 当前房间排行榜只展示个人参与分。
- 座位分只在内部维护，不在排行榜展示。

### 20.3 WebSocket 风险

- 小程序端可能断线。
- 必须实现断线重连。
- 重连后必须拉取完整 snapshot。
- WebSocket 只作为通知通道，最终状态以 REST snapshot 为准。

---

## 21. 推荐最终技术决策

最终采用：

```text
前端：
uni-app + Vue 3 + Pinia + 自定义蓝白 UI + SocketTask

后端：
Java 17 + Spring Boot 3.x + Maven + MyBatis-Plus + MySQL 8 + JWT + Spring WebSocket

缓存：
MVP 不强依赖 Redis，后续预留

部署：
微信小程序端 + Java 后端服务器 + MySQL
```

---

## 22. 一句话总结

这个项目的本质是：

> 一个以“座位继承”为核心模型、以“用户实际参与轮次”为个人统计口径、以 WebSocket 实现多人实时同步的麻将计分微信小程序。

